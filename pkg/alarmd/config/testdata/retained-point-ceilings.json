{
  "what": "How many retained history points fit one stored Runtime State record, by how many Levels share it. Index is the Level count; index 0 is unused and index 9 is the extra Level a no-data Plan compiles beside the eight a strategy may declare.",
  "why_committed": "These numbers are derived, so nothing in review shows them moving. A change to the stored representation, to the encoder's per-point cost, or to max_encoded_bytes moves the whole table silently, and the failure it produces is a per-series write refusal with nothing to say why. Committed here so the move arrives as a diff a reader has to agree with.",
  "inputs": {
    "max_encoded_bytes": 524288,
    "max_required_history_points": 4096,
    "max_levels_per_plan": 8
  },
  "packed": {
    "representation": "ALD2 framed record, the one the store writes",
    "derived_by": "state.MaxPackedFramePoints(levelCount, max_encoded_bytes)",
    "ceilings": [0, 39968, 39922, 39876, 39831, 39785, 39740, 39694, 39649, 32178],
    "notes": [
      "A Level costs three bits per point here - the present, valid and anomalous bitmaps - so the table barely falls as Levels are added.",
      "The step at nine Levels is the bitmaps widening from one byte to two, which costs three bytes on every point at once. It is a property of the encoding, not of the configuration.",
      "Every entry is far above max_required_history_points, so the configured limit is the binding one at every Level count. That is the end state decision-021 was for: before it, the ceiling bound first and was written nowhere."
    ]
  },
  "envelope": {
    "representation": "the JSON runtime envelope, what the store wrote before decision-021",
    "derived_by": "state.MaxRuntimeEnvelopePoints(levelCount, max_encoded_bytes)",
    "ceilings": [0, 2087, 1395, 1047, 838, 698, 598, 523, 464, 418],
    "notes": [
      "Kept for the comparison rather than because anything reads it. A single-Level Plan went from 2087 points to 39968, and an eight-Level one from 464 to 39649 - the second is the ratio that matters, because a crowded record is where the old ceiling bit first.",
      "Under this table a Plan configured at the permitted 4096 points compiled, activated, and then had every state write refused per series."
    ]
  }
}
